import { SxProps, Theme } from '@mui/material/styles';
import { theme } from '../../styles/materialTheme';

export const topBarButtonSx = {
  marginRight: theme.spacing(2),
  color: 'text.secondary',
} as SxProps<Theme>;
