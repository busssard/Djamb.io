import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    mode: 'dark',
  },
  typography: {
    h4: {
      color: '#fff',
    },
    h5: {
      color: '#fff',
    },
    body1: {
      color: '#ddd',
    },
    caption: {
      color: '#fff',
      opacity: 0.7,
    },
  },
});
